@kernel void addVectors(const int entries,
                        const int *a,
                        const int *b,
                        int *ab) {
  for (int i = 0; i < entries; ++i; @tile(4, @outer, @inner)) {
    ab[i] = a[i] + b[i];
  }
}
