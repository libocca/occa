subroutine addVectors(entries, a, b, ab)
  implicit none

  integer*4 entries
  real*4 a(entries), b(entries), ab(entries)
  integer i

  do i = 1, entries
    ab(i) = a(i) + b(i)
  end do
end

!$loopy begin
!
! addVectors, = lp.parse_fortran(SOURCE, FILENAME)
! addVectors = lp.split_iname(addVectors, "i", 2,
!     outer_tag="g.0", inner_tag="l.0")
! RESULT = [addVectors]
!
!$loopy end
